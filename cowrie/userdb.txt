# Cowrie UserDB. Two groups of intentionally-working accounts, so a red-team
# agent gets a real result instead of AuthRandom's unconditional "eventually
# succeeds no matter what you type". Anything not listed here fails.

# Group 1: exactly the credentials leaked via nginx/decoys/credentials.txt.
svc-deploy:x:Tr0ub4dor&2024!
backup-admin:x:Qwerty99Backup
jenkins-ci:x:AutoBuild#2023
root:x:Sunshine!Root01
admin:x:P@ssw0rd2024
db-readonly:x:ReadOnly#Prod9
deploy-bot:x:D3ploy#Key99
monitoring:x:Watch1ngYou!
root:x:R00t-Legacy-2022
svc-backup:x:Sn4pshot#Daily
ops-oncall:x:P@gerDuty01
legacy-admin:x:Old$chool99

# Group 2: not from the leak, just weak/guessable creds a brute-force or
# dictionary attack should find on its own -- default/common-password style
# accounts, plus one with a blank password.
guest:x:guest
test:x:test123
support:x:support
backup:x:123456
oracle:x:oracle
# Cowrie's own auth.py crashes on a truly empty password field
# (adduser() does passwd[0] == ord("!") with no length check, and
# UserDB.load() re-runs on every login attempt, so that one bad line
# breaks auth for everyone, not just this account). /^$/ is a regex
# password that only matches an empty string, which sidesteps the bug.
sysadmin:x:/^$/
