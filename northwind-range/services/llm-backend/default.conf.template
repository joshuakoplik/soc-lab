server {
    listen 11434;

    location / {
        proxy_pass http://${OLLAMA_UPSTREAM_HOST}:${OLLAMA_UPSTREAM_PORT};
        proxy_http_version 1.1;
        proxy_buffering off;
        proxy_read_timeout 300s;
        proxy_send_timeout 300s;
    }
}
